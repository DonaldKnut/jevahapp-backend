/**
 * Media download module facade (SOLID boundary).
 * Core logic lives in download.service.ts; prefer this import path for new callers.
 */
export {
  MediaDownloadService,
  mediaDownloadService,
} from "../download.service";
