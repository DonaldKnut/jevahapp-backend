/**
 * Notification module facade (SOLID boundary).
 * Implementation remains in notification.service.ts; new code should import from here.
 */
export { NotificationService } from "../notification.service";
export type { CreateNotificationData } from "../notification.service";
